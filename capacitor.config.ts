import type { CapacitorConfig } from '@capamohcitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tertius.integrity',
  appName: 'Tertius Integrity AI',
  webDir: 'dist'
};

export default config;
